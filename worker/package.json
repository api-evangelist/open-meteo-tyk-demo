{
  "name": "open-meteo-tyk-demo-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: public proxy + demo page for Open-Meteo weather & air quality",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
